# n8n Configuration
# Copy this file to .env and update with your values

# Server Configuration
N8N_HOST=localhost
N8N_PORT=5678

# Basic Authentication
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=admin

# Webhook Paths
# These paths will be used to create webhook URLs
# Full URL format: http://localhost:5678/webhook/{path}
N8N_BOOK_WEBHOOK_PATH=/webhook/book
N8N_CANCEL_WEBHOOK_PATH=/webhook/cancel

# Environment
NODE_ENV=production

# Note: After starting n8n, you'll need to:
# 1. Access n8n UI at http://localhost:5678
# 2. Import the workflow JSON files from ./workflows/
# 3. Activate the workflows
# 4. Copy the webhook URLs and configure them in backend/.env as:
#    N8N_BOOK_WEBHOOK_URL=http://n8n:5678/webhook/book
#    N8N_CANCEL_WEBHOOK_URL=http://n8n:5678/webhook/cancel
