services:
  backend:
    build: ./backend
    container_name: chatbot-backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
      - ./n8n/.env
    restart: unless-stopped
    networks:
      - chatbot-network

networks:
  chatbot-network:
    external: true
  